import { WeightAndQuantityValidatorDirective } from './weight-and-quantity-validator.directive';

describe('WeightAndQuantityValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new WeightAndQuantityValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
